
describe('key manager service test', () => {
 /* test('test generate mnemonic', async () => {
      const mnemonic = keyringService.generateMnemonic();
      expect(mnemonic).toBeDefined();
      const mnemonicArray = mnemonic.split(' ');
      expect(mnemonicArray).toHaveLength(12);
  }); */
  test('test keyring lock', async () => {
    //keyringService.setLocked();
    //expect(keyringService.password).toBeNull();
    //expect(keyringService.keyrings).toHaveLength(0);
  });
});

export {};
