import React from 'react';

export default function TestTxList() {
  // const { transactions, recopyItemsAndAppend } = useTransactionHistory()
  return (
    <div className="list h-full">
      {/* <Button onClick={recopyItemsAndAppend}>More Items</Button> */}
      {/* <TransactionsList transactions={transactions} /> */}
    </div>
  );
}
